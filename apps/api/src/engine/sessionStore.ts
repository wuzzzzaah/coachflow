// ISessionStore is defined in @coachflow/shared so the contract suite can import it
// without a cross-package dependency. Re-export here so the engine imports a single path.
export type { ISessionStore } from '@coachflow/shared';
